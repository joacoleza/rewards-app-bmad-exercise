/** Placeholder types — expanded in subsequent stories */

/** Application environment */
export type Environment = 'development' | 'staging' | 'production';

/** Health check response */
export interface HealthCheckResponse {
  status: 'ok';
}
